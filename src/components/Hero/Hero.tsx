'use client';

import HeroOverlay from './HeroOverlay';

export default function Hero() {
  return (
    <section className="relative min-h-[75vh] flex items-center justify-center pt-20 pb-10">
      <HeroOverlay />
    </section>
  );
}
